export const testUtils = async ({ page }) => {
  page.addInitScript(() => {
    window.localStorage.setItem('telemetryAccepted', 'false');
  });
  await page.evaluate(() => document.fonts.status);
};
