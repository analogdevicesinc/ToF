import { test } from '@playwright/test';
import { testUtils } from '../../../utils/test.util';
test.describe('Progress Bar Snapshot Test', () => {
  test.beforeEach(async ({ page }) => {
    await testUtils({ page });
  });
});
