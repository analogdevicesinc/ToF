import { newSpecPage } from '@stencil/core/testing';
import { HmcDropdownMenuButton } from '../dropdown-menu-button';
describe('hmc-dropdown-menu', () => {
  it('renders', async () => {
    const page = await newSpecPage({
      components: [HmcDropdownMenuButton],
      html: `<hmc-dropdown-menu-button ></hmc-dropdown-menu-button>`,
    });
    expect(page.root).toMatchInlineSnapshot();
  });
});
