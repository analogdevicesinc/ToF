import { newSpecPage } from '@stencil/core/testing';
import { HmcNavBar } from '../nav-bar/nav-bar';
describe('hmc-nav-bar', () => {
  it('builds', async () => {
    expect(new HmcNavBar()).toBeTruthy();
  });
  describe('renders', () => {
    it('should render default component', async () => {
      const { root } = await newSpecPage({
        components: [HmcNavBar],
        html: '<hmc-nav-bar></hmc-nav-bar>',
      });
      expect(root).toMatchInlineSnapshot();
    });
  });
});
