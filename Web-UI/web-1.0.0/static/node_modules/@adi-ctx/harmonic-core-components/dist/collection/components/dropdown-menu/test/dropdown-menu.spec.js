import { newSpecPage } from '@stencil/core/testing';
import { HmcDropdownMenu } from '../dropdown-menu';
describe('hmc-dropdown-menu', () => {
  it('renders', async () => {
    const page = await newSpecPage({
      components: [HmcDropdownMenu],
      html: `<hmc-dropdown-menu ></hmc-dropdown-menu>`,
    });
    expect(page.root).toMatchInlineSnapshot();
  });
});
