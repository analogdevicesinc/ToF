import { newSpecPage } from '@stencil/core/testing';
import { HmcNavLink } from '../nav-link/nav-link';
describe('hmc-nav-link', () => {
  let page;
  beforeEach(async () => {
    page = await newSpecPage({
      components: [HmcNavLink],
    });
  });
  it('builds', async () => {
    expect(new HmcNavLink()).toBeTruthy();
  });
  describe('renders', () => {
    it('should render default component', async () => {
      await page.setContent('<hmc-nav-link></hmc-nav-link>');
      expect(page.root).toMatchInlineSnapshot();
    });
  });
});
