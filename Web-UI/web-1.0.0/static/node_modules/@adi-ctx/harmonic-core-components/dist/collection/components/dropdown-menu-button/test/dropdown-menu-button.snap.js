import { test, expect } from '@playwright/test';
import { testUtils } from '../../../utils/test.util';
test.describe('Dropdown Menu Button Snapshot Test', () => {
  test.beforeEach(async ({ page }) => {
    await testUtils({ page });
  });
  test('Default', async ({ page }) => {
    await page.goto('/iframe.html?args=&id=components-dropdown-menu-button--default&viewMode=story');
    await expect(page.locator('hmc-dropdown-menu-button')).toHaveScreenshot();
  });
});
