/// <reference types="react" />
/**
 * @slot - Unnamed slot is used to pass in the panel's content.
 */
export declare class HmcTabPanel {
  el: HTMLElement;
  private _id;
  /**
   * Specifies internal name of the tab.
   * It is used for connecting the panel to the controlling tab.
   */
  name: string;
  /**
   * Tab panel will only be visible if this is set `true`.
   * Default: 'false'.
   */
  isSelected: boolean;
  handleKeyEvent(ev: KeyboardEvent): void;
  componentWillLoad(): void;
  render(): JSX.Element;
}
