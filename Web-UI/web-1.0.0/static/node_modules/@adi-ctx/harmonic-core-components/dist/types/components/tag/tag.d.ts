/// <reference types="react" />
import { IconNames } from '../icon/iconsMap';
import { TagSize } from '../type/size.type';
import { TagStatusType } from '../type/status.type';
export declare class HmcTag {
  /**
   * The amount of emphasis in the UI that the tag has.
   * Available options are: 'subtle', and 'strong'
   * Default: 'subtle'.
   */
  emphasis: 'subtle' | 'strong';
  /**
   * The size of the tag.
   * Available options are: 'sm', and 'md'
   * Default: 'md'.
   */
  size: TagSize;
  /**
   * If enabled, input will be loaded with 'width: 100%'.
   * Default, `false`.
   */
  isFullWidth: boolean;
  /**
   * Specifies status prop
   * that provides related status ('success' | 'warning' | 'error' | 'info' | 'highlight) with its styles
   * Default: 'info'
   */
  status?: TagStatusType;
  /**
   * Toggles the display of an icon to the left of the tag main text.
   */
  withIcon: boolean;
  /**
   * Specifies iconName prop
   * that provides icon from hmc-core-components icons.
   * If not specified, the icon name will be taken from the status name.
   */
  iconName: IconNames;
  render(): JSX.Element;
}
