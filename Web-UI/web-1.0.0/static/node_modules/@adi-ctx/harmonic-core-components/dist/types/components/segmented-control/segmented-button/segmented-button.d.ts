/// <reference types="react" />
import { EventEmitter } from '../../../stencil-public-runtime';
import { SegmentedButtonSize } from '../../type/size.type';
/**
 * @slot - An unnamed slot is used to pass a content (usually text) to the component.
 */
export declare class HmcSegmentedButton {
  focusEl: HTMLElement;
  el: HTMLHmcSegmentedButtonElement;
  /**
   * Specifies if SegmentedButton is pressed/active.
   */
  isPressed: boolean;
  /**
   * If `true`, the user cannot interact with the segment button.
   */
  isDisabled: boolean;
  /**
   * If `true`, the tab will be loaded with 'width: 100%'.
   * Default: 'false'.
   */
  isFullWidth: boolean;
  /**
   * If enabled, the button will be loaded with an only icon inside.
   * Icon should be set via default slot.
   * Default: `false`.
   */
  isIconOnly: boolean;
  /**
   * The value of the Segment Button.
   */
  value: string;
  /**
   * The size of the Segment Button.
   * Available options are: "xs", "sm" and "md" . Default = "md".
   */
  size: SegmentedButtonSize;
  /**
   * Event emitter that is triggered when any item is clicked
   */
  segmentClicked: EventEmitter<string>;
  /**
   * State determined by detecting siblings on load.
   */
  isFirstElement: boolean;
  isLastElement: boolean;
  /**
   * The tabindex of the radio button.
   */
  buttonTabindex: number;
  setFocus(ev: any): Promise<void>;
  onClick(): void;
  connectedCallback(): void;
  render(): JSX.Element;
}
