import { BadgeVariant } from '../type/variant.type';
import { BadgeStatusType } from '../type/status.type';
export declare class HmcBadge {
  /**
   * The kind of the badge.
   * Available options are: 'digits', and 'dots'
   * Default: 'digits'.
   */
  kind: BadgeVariant;
  /**
   * The status of the badge.
   * Available options are: 'urgent', and 'information'
   * Default: 'urgent'.
   */
  status: BadgeStatusType;
  /**
   * The label of the badge.
   * Number can only take non-negative integer numbers.
   * Default: 1.
   */
  number?: number;
  /**
   * The max number of the badge.
   * Maximum number to appear on the label.
   * A number greater than the max number will appear in the label as '{maxNumber}+'.
   * Maximum Number can only take non-negative integer numbers.
   * Default: 999.
   */
  maxNumber?: number;
  componentWillRender(): void;
  private fixNumberInteger;
  private getLabel;
  render(): any;
}
