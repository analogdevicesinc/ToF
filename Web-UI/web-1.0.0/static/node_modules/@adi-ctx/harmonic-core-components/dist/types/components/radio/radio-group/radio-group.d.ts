/// <reference types="react" />
import { EventEmitter } from '../../../stencil-public-runtime';
/**
 * @slot - An unnamed slot is used to pass a hmc-radio-buttons to the component.
 */
export declare class HmcRadioGroup {
  el: HTMLElement;
  /**
   * 'radioChanged' event emitted when user clicks on particular radio button
   * and value of that radio is passed as a param.
   */
  radioChanged: EventEmitter<string>;
  /**
   * The name of the control, which is submitted with the form data.
   */
  name: string;
  /**
   * the value of the radio group.
   */
  value?: any | null;
  valueChanged(value: any | undefined): void;
  private getRadios;
  private onClick;
  onKeydown(ev: any): void;
  render(): JSX.Element;
  private setActiveItem;
}
