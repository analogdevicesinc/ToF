/// <reference types="react" />
import { EventEmitter } from '../../stencil-public-runtime';
import { DropdownOptionSize } from '../type/size.type';
/**
 * @slot - An unnamed slot is used to pass title label of Dropdown Option.
 * @slot caption - Slot to pass in caption to appear on below title label.
 * @slot left-icon - Slot to pass in hmc-icon to appear on left.
 * @slot right-icon - Slot to pass in hmc-icon to appear on right.
 * @slot left-value - Slot to pass Value to appear on left for Action type.
 * @slot right-value - Slot to pass Value to appear on right for Action type.
 */
export declare class HmcDropdownOption {
  el: HTMLElement;
  /**
   * The value of the option.
   */
  value: any;
  /**
   * This prop changes the look of the option depending on whether it is for use with a multiple
   * or single selection dropdown.
   * Default, `false`.
   */
  isMultiple: boolean;
  /**
   * This prop changes the look of the option depending on whether it is for use with an action.
   * Default, `false`.
   */
  isAction: boolean;
  /**
   * Specifies 'selected' state of the option.
   * Default, `false`.
   */
  isSelected: boolean;
  /**
   * Specifies 'disabled' state of the option.
   * Default, `false`.
   */
  isDisabled: boolean;
  /**
   * The size of the input.
   * Available options are: "xs", "sm" and "md".
   * Default: "md".
   */
  size: DropdownOptionSize;
  selection: EventEmitter;
  role: string;
  connectedCallback(): void;
  setRoleBasedOnAncestor(): void;
  handleItemSelect: (event: MouseEvent) => void;
  renderSingleSelectOption(): any;
  renderMultiSelectOption(): any;
  renderActionOption(): any;
  renderSelectionIndicator(): any;
  componentDidLoad(): void;
  render(): JSX.Element;
  isLastChild(): boolean;
}
