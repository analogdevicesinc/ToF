import { ProgressBarStatusType } from '../type/status.type';
/**
 * @slot action-button - Slot to pass in hmc-button to appear on right of the content.
 */
export declare class HmcProgressBar {
  /**
   * Specifies Label text of the Progress Bar. If no text is entered, Label section will not be displayed.
   * Default: undefined.
   */
  label: string | undefined;
  /**
   * Specifies Inline Message text of the Progress Bar. If no text is entered, the inline message section will not be displayed.
   * Default: undefined.
   */
  inlineMessage: string | undefined;
  /**
   * The value being displayed by the bar, relative to maxValue.
   * Default: 0.
   */
  value: number;
  /**
   * The maximum allowed value, this determines how filled the bar is for a given value.
   * Default: 100.
   */
  maxValue: number;
  /**
   * The status of the Progress Bar.
   * Available options are: 'info', 'success', and 'error'
   * Default: 'info'.
   */
  status: ProgressBarStatusType;
  /**
   * The size of the Progress Bar.
   * Available options are: 'sm', and 'md'
   * Default: 'md'.
   */
  size: 'sm' | 'md';
  /**
   * The kind of the Progress Bar.
   * If enabled, the Progress Bar will be indeterminate type otherwise is determinate.
   * Default: `false`.
   */
  indeterminate: boolean;
  /**
   * Specifies whether the percentage text of progress value of the Progress Bar will be shown or not.
   * Default: `true`.
   */
  showPercentage: boolean;
  el: HTMLElement;
  private getConstrainedValue;
  render(): void;
}
