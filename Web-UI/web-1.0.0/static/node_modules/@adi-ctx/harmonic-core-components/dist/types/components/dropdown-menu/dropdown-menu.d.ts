/// <reference types="react" />
import { Instance } from '@popperjs/core/lib/popper-lite';
/**
 * @slot - An unnamed slot is used to pass a content to the DropdownMenu.
 */
export declare class HmcDropdownMenu {
  el: HTMLElement;
  menuInstance: Instance;
  triggerElement: HTMLElement;
  menuEl: HTMLDivElement;
  /**
   * Specifies id of the element (triggerElement) that will be show/hide tooltip.
   */
  triggerId: string;
  /**
   * Use to programatically set the initial open state of the tooltip.
   * Default: 'false'.
   */
  isOpen: boolean;
  componentDidLoad(): void;
  selectMenuTarget(): void;
  handleMenu: () => void;
  render(): JSX.Element;
}
