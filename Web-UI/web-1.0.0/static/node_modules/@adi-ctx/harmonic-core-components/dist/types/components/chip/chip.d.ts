/// <reference types="react" />
import { EventEmitter } from '../../stencil-public-runtime';
import { ButtonSize } from '../type/size.type';
/**
 * @slot icon - Slot to pass in hmc-icon to appear on left.
 * @slot - An unnamed slot is used to pass a text label to the chip.
 */
export declare class HmcChip {
  el: HTMLElement;
  /**
   * The size of the chip.
   * Available options are: "xs" - h-24, "sm" - h-32, "md" - h-40.
   * Default: "md".
   */
  size: ButtonSize;
  /**
   * Specifies 'disabled' state of the chip.
   * Default: `false`.
   */
  isDisabled: boolean;
  /**
   * Specifies whether the chip should have input focus when the page loads.
   * Default: `false`.
   */
  autofocus: boolean;
  /**
   * If true, adds a checkmark to the chip when it is selected.
   */
  hasCheckmark: boolean;
  /**
   * Specifies whether the chip is selected.
   * Default: `false`.
   */
  isSelected: boolean;
  /**
   * Fires when user clicks the chip and the selection state is toggled.
   */
  hmcChange: EventEmitter<{
    selected: boolean;
  }>;
  private clickHandler;
  render(): JSX.Element;
}
