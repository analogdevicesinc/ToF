import { SpinnerSize } from '../type/size.type';
import { SpinnerVariant } from '../type/variant.type';
/**
 * @slot - An unnamed slot is used to pass in visually hidden text for screen readers.
 */
export declare class HmcSpinner {
  /**
   * The size of the spinner.
   * Available options are: 'sm', and 'md'
   * Default: 'sm'.
   */
  size: SpinnerSize;
  /**
   * The kind of spinner.
   * Available options are: 'primary', 'secondary' and 'tertiary'
   * Default: 'primary'.
   */
  kind: SpinnerVariant;
  render(): any;
}
