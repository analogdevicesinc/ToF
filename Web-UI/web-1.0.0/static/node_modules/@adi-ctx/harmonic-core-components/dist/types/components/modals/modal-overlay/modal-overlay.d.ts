/// <reference types="react" />
import { EventEmitter } from '../../../stencil-public-runtime';
/**
 * @slot - An unnamed slot is used to pass child nodes to the ModalOverlay component.
 * All children will be overlaid by ModalOverlay in case of isOpen prop is equal to true.
 */
export declare class HmcModalOverlay {
  /**
   * Specifies isOpen prop, if `true`, the overlay will be visible.
   * Default: false
   */
  isOpen: boolean;
  /**
   * 'overlayClick' event emitted when a user clicks on a overlay
   * listens to this event to handle self-closing.
   */
  overlayClick: EventEmitter;
  onOverlayClickHandler({ target }: MouseEvent): void;
  overlayEl: HTMLDivElement;
  render(): JSX.Element;
}
