/// <reference types="react" />
import { EventEmitter } from '../../stencil-public-runtime';
import { AccordionSize } from '../type/size.type';
/**
 * @slot header - Slot="header" is used to pass a header content to the Accordion.
 * @slot content - Slot="content" is used to pass content to the Accordion, which will be visible when isOpen is true.
 */
export declare class HmcAccordion {
  el: HTMLElement;
  /**
   * Specifies isOpen prop, if `true`, the panel will be opened.
   * Default: false.
   */
  isOpen: boolean;
  /**
   * The size of the accordion header.
   * Available options are: "xs" and "md".
   * Default: "md".
   */
  size: AccordionSize;
  /**
   * 'accordionChange' event emitted when user clicks on the header
   * and the panel visibility is toggled.
   */
  accordionChange: EventEmitter<{
    open: boolean;
  }>;
  watchIsOpenHandler(newValue: boolean): void;
  handleFocusIn(ev: any): void;
  container: HTMLElement;
  componentDidLoad(): void;
  togglePanel(): void;
  handleSetContainerHeight(timeoutDuration: number, shouldPanelOpen: boolean, desiredHeight: string | null): void;
  handlePanelCollapsing(): void;
  render(): JSX.Element;
}
