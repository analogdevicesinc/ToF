/// <reference types="react" />
import { EventEmitter } from '../../../stencil-public-runtime';
import { TabSize } from '../../type/size.type';
/**
 * @slot - Unnamed slot is used to pass a tab title to the component.
 */
export declare class HmcTab {
  el: HTMLElement;
  /**
   * Specifies internal name of the tab.
   * It used in work around setting/getting currently active tab
   */
  panel: string;
  /**
   * If `true`, the user cannot interact with the tab.
   * Default: 'false'.
   */
  isDisabled: boolean;
  /**
   * Tab panel will only be visible if this is set `true`.
   * Default: 'false'.
   */
  isSelected: boolean;
  /**
   * If `true`, the tab will be loaded with 'width: 100%'.
   * Default: 'false'.
   */
  isFullWidth: boolean;
  /**
   * The size of the tab. Determines font size and line height.
   * Available options are: "xs", "sm" and "md".
   * Default: "md".
   */
  size: TabSize;
  /**
   * 'tabClicked' event emitted when user clicks on the particular
   * tab and its name is passed as a param
   */
  tabClicked: EventEmitter<string>;
  clickHandler(): void;
  render(): JSX.Element;
}
