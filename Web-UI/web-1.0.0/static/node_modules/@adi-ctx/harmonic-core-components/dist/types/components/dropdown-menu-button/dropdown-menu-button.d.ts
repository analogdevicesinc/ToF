/// <reference types="react" />
import { EventEmitter } from '../../stencil-public-runtime';
import { IconNames } from '../../components';
import { ButtonSize } from '../type/size.type';
import { VariantType } from '../type/variant.type';
/**
 * @slot icon-left - Slot to pass in hmc-icon to appear on left.
 * @slot - An unnamed slot is used to pass a text label to the button.
 * @slot menu - Slot to pass in hmc-dropdown-menu to appear when opened.
 */
export declare class HmcDropdownMenuButton {
  el: HTMLElement;
  /**
   * Specifies 'disabled' state of the component.
   * Default, `false`.
   */
  isDisabled: boolean;
  isLoading: boolean;
  /**
   * The kind of the button.
   * Available options are: 'primary', 'secondary', 'tertiary' and 'ghost'.
   * Default: 'primary'.
   */
  kind: VariantType;
  isInverse: boolean;
  /**
   * The size of the Dropdown Menu Button.
   * Available options are: "xs" - h-24, "sm" - h-32, "md" - h-40.
   * Default: "md".
   */
  size: ButtonSize;
  isExpanded: boolean;
  /**
   * Emitted when the dropdown menu button's expanded state is toggled.
   * Event detail: { isExpanded: boolean }.
   */
  toggled: EventEmitter<{
    isExpanded: boolean;
  }>;
  connectedCallback(): void;
  disconnectedCallback(): void;
  handleDocumentClick: (event: MouseEvent) => void;
  toggleMenu(): Promise<void>;
  getRightIcon(): IconNames;
  render(): JSX.Element;
}
