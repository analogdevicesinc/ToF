/// <reference types="react" />
import { EventEmitter } from '../../stencil-public-runtime';
import { ToggleSize } from '../type/size.type';
export declare class HmcToggle {
  focusEl: HTMLInputElement;
  el: HTMLElement;
  _focused: boolean;
  /**
   * Set toggle's label
   * Default: undefined
   */
  label: string;
  /**
   * If `true`, the toggle is selected.
   * Default: `false`.
   */
  isChecked: boolean;
  /**
   * Specifies 'disabled' state of the toggle.
   * Default: `false`.
   */
  isDisabled: boolean;
  /**
   * If full-width is set to true. The toggle will fill the width of its parent.
   * Default: `false`.
   */
  isFullWidth: boolean;
  /**
   * Specifies size of component, reflected in label font.
   * Default: "md".
   */
  size: ToggleSize;
  /**
   * The name of the control, which is submitted with the form data.
   */
  name: string;
  /**
   * The value of the toggle does not mean if it's checked or not, use the `isChecked`
   * property for that.
   *
   * The value of a toggle is analogous to the value of an `<input type="checkbox">`,
   * it's only used when the toggle participates in a native `<form>`.
   */
  value: any | null;
  /**
   * Emitted when the toggle has changed state.
   */
  changed: EventEmitter<{
    checked: boolean;
  }>;
  checkedChanged(isChecked: boolean): void;
  private setFocus;
  private onClick;
  render(): JSX.Element;
}
