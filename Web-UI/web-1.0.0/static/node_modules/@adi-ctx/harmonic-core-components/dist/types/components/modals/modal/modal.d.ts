/// <reference types="react" />
import { EventEmitter } from '../../../stencil-public-runtime';
/**
 * @slot modal-header - slot="modal-header" is used to pass header content to the Modal.
 * @slot - An unnamed slot is used to pass a main content to the Modal.
 * @slot modal-footer - slot="modal-footer" is used to pass footer content to the Modal.
 */
export declare class HmcModal {
  el: HTMLElement;
  _startTrapRef: HTMLAnchorElement;
  _endTrapRef: HTMLAnchorElement;
  /**
   * Specifies isOpen prop for both modal and overlay.
   * Default: false
   */
  isOpen: any;
  /**
   * Specifies isFullScreen prop that set full screen size of modal.
   * Default: false
   */
  isFullScreen: boolean;
  /**
   * If 'true', the modal will request to be dismissed when the overlay is clicked.
   * Default: true
   */
  overlayDismiss: boolean;
  /**
   * Specifies the density of information in the header. Similar to the size prop in other components.
   * Specifying 'medium' will display a smaller padding allowing more information to be conveyed.
   * Default: 'low'
   */
  density?: 'medium' | 'low';
  /**
   * Specifies scroll position after close modal.
   * Default: 0
   */
  scrollY: number;
  hasHeader: boolean;
  hasFooter: boolean;
  label: string;
  /**
   * 'modalCloseRequest' event emitted when user click on overlay or close icon.
   */
  modalCloseRequest: EventEmitter;
  componentWillLoad(): void;
  disconnectedCallback(): void;
  private watchHandler;
  overlayClickHandler(): void;
  onKeydown(ev: KeyboardEvent): void;
  private handleModalClose;
  private removeClassModalOpen;
  private wrapFocus;
  render(): JSX.Element;
}
