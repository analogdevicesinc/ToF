/// <reference types="react" />
import { EventEmitter } from '../../../stencil-public-runtime';
/**
 * @slot - An unnamed slot is used to pass `hmc-tab` components to the Tab Group.
 * @slot panel - Used to pass in hmc-panel components which are displayed depending on which tab is selected.
 */
export declare class HmcTabGroup {
  el: HTMLElement;
  activeTab: HTMLHmcTabElement;
  /**
   * 'tabSelected' event emitted when user clicks on header of particular tab
   * and name of that tab is passed as param
   */
  tabSelected: EventEmitter<string>;
  tabClickedHandler(event: CustomEvent<string>): void;
  onKeydown(ev: any): void;
  private get panels();
  private get tabs();
  componentWillLoad(): void;
  linkTabsToPanels(): void;
  render(): JSX.Element;
  private getActiveTab;
  private setActiveItem;
}
