/// <reference types="react" />
import { Instance } from '@popperjs/core/lib/popper-lite';
import { TooltipPlacement } from '../type/variant.type';
import { TooltipSize } from '../type/size.type';
/**
 * @slot - An unnamed slot is used to pass content to the Tooltip component.
 */
export declare class HmcTooltip {
  el: HTMLElement;
  /**
   * Specifies id of the element (triggerElement) that will be show/hide tooltip.
   */
  triggerId: string;
  /**
   * Specifies placement where tooltip will be render relatively to the triggerElement.
   * Default: 'bottom'.
   */
  placement: TooltipPlacement;
  /**
   * Adds a title to a toggletip element.
   * Default: `''`
   */
  toggleTitle: string;
  /**
   * Use to programatically set the initial open state of the tooltip.
   * Default: 'false'.
   */
  isOpen: boolean;
  /**
   * Specifies whether the component fits content or is fixed width.
   * Default: 'false'.
   */
  isFixedWidth: boolean;
  /**
   * Specifies whether the component is a toggletip.
   * Default: 'false'.
   */
  isToggle: boolean;
  /**
   * Specifies the size of the tooltip.
   * Available options are: "xs", "sm".
   * Default: 'sm'.
   */
  size: TooltipSize;
  updateOnTriggerIdChange(): void;
  closeTooltip(ev: KeyboardEvent): void;
  tooltipInstance: Instance;
  triggerElement: HTMLElement;
  tooltipEl: HTMLDivElement;
  componentDidUpdate(): void;
  componentDidLoad(): void;
  selectTooltipTarget: () => void;
  handleTooltip: () => void;
  render(): JSX.Element;
}
