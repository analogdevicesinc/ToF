/// <reference types="react" />
import { RadioSize } from '../../type/size.type';
/**
 * @slot - An unnamed slot is used to pass a label to the component.
 */
export declare class HmcRadioButton {
  private inputId;
  private radioGroup;
  private nativeInput;
  el: HTMLElement;
  /**
   * If `true`, indicates that this radio button is selected one in the group.
   */
  isChecked: boolean;
  /**
   * Specifies internal value of the radio button.
   * It used in setting/getting currently checked radio and emitting checked radio button's value.
   */
  value: string;
  /**
   * Optional label to put beside button.
   */
  label?: string;
  /**
   * The size of the radio button.
   * Available options are: "xs" and "md".
   * Default: "md".
   */
  size: RadioSize;
  /**
   * Specifies 'disabled' state of the radio button.
   * Default, `false`.
   */
  isDisabled: boolean;
  /**
   * The tabindex of the radio button.
   */
  buttonTabindex: number;
  setFocus(ev: any): Promise<void>;
  setButtonTabindex(value: number): Promise<void>;
  private updateState;
  connectedCallback(): void;
  disconnectedCallback(): void;
  private onClick;
  render(): JSX.Element;
}
