To build this on JetPack 6.2 the following must be run:

```console
sudo apt update
sudo apt install pkg-config libssl-dev

const VERSION_URL: &str = "https://swdownloads.analog.com/cse/aditof/aware3d/sw_update/adcam-mipi.json";